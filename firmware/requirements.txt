# Target: Raspberry Pi OS Bookworm (64-bit) on Raspberry Pi 5
# picamera2 and PyQt5 are best installed via apt on Raspberry Pi OS:
#   sudo apt install -y python3-picamera2 python3-pyqt5 python3-opencv
# The rest via pip (inside a venv created with --system-site-packages):
numpy
opencv-python-headless; platform_machine != "aarch64"
Pillow>=10.0
flask>=3.0
qrcode[pil]
gpiozero
imageio
